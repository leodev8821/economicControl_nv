// packages/server/.sequelizerc
const path = require('path');

module.exports = {
  // USAMOS DIST para la configuración (donde los .js sí existen físicamente)
  'config': path.resolve(__dirname, 'dist', 'config', 'sequelize.config.js'),
  
  // USAMOS SRC para las migraciones (porque son .cjs y no se mueven a dist)
  'migrations-path': path.resolve(__dirname, 'src', 'migrations'),
  
  'seeders-path': path.resolve(__dirname, 'src', 'seeders'),
  'models-path': path.resolve(__dirname, 'dist', 'models')
};